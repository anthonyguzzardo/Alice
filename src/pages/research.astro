---
/**
 * Research page: the problem Alice solves, grounded in science.
 *
 * Genre: "problem page" -- lead with indisputable data, build the
 * logical argument, let the product emerge as the obvious conclusion.
 * No pitch. No CTA. The reader finishes and knows what needs to exist.
 *
 * Sources: Guzzardo 2026a ("A Closing Window"), Guzzardo 2026b
 * ("Construct Replacement"), and the primary literature they cite.
 * All numbers are real. All citations are traceable.
 */
import PublicLayout from '../layouts/layPublic.astro';
---

<PublicLayout title="Alice -- Research" active="research" description="The scientific case for a cognitive instrument built on unmediated writing.">

<!-- ===== HERO ===== -->
<header class="r-hero">
  <div class="r-hero-text">
    <h1>The behavioral sciences have a data problem they haven't named yet.</h1>
    <p class="r-subtitle">AI is changing human behavior faster than the fields that measure it can adapt. The window for capturing unmediated cognitive baselines is closing. This page explains why that matters and what it takes to build an instrument that survives the shift.</p>
  </div>
  <div class="r-hero-viz">
    <canvas id="window-viz" width="480" height="360" aria-hidden="true"></canvas>
    <div class="r-hero-viz-caption">
      <span class="r-viz-label-left">Motor noise declining</span>
      <span class="r-viz-label-right">AI contamination rising</span>
    </div>
  </div>
</header>

<div class="r-layout">

  <!-- ===== SIDEBAR NAV ===== -->
  <nav class="r-nav" aria-label="Page sections">
    <div class="r-nav-group">
      <span class="r-nav-group-label">The Problem</span>
      <a href="#evidence" class="r-nav-link" data-section="evidence">The evidence base</a>
      <a href="#population" class="r-nav-link" data-section="population">The wrong population</a>
      <a href="#window" class="r-nav-link" data-section="window">The closing window</a>
      <a href="#construct" class="r-nav-link" data-section="construct">Construct replacement</a>
    </div>
    <div class="r-nav-group">
      <span class="r-nav-group-label">The Shift</span>
      <a href="#precedent" class="r-nav-link" data-section="precedent">Clock-drawing precedent</a>
      <a href="#paradigm" class="r-nav-link" data-section="paradigm">Self-referential baselines</a>
      <a href="#gap" class="r-nav-link" data-section="gap">The instrument gap</a>
    </div>
    <div class="r-nav-group">
      <span class="r-nav-group-label">The Instrument</span>
      <a href="#writing" class="r-nav-link" data-section="writing">Why writing</a>
      <a href="#landscape" class="r-nav-link" data-section="landscape">Existing instruments</a>
      <a href="#observer" class="r-nav-link" data-section="observer">The observer effect</a>
      <a href="#design" class="r-nav-link" data-section="design">Validity requirements</a>
      <a href="#extensions" class="r-nav-link" data-section="extensions">Theoretical extensions</a>
      <a href="#open" class="r-nav-link" data-section="open">Open questions</a>
    </div>
  </nav>

  <!-- ===== CONTENT ===== -->
  <article class="research">

  <!-- ===== SECTION 1: THE EVIDENCE BASE IS THINNER THAN IT LOOKS ===== -->
  <section class="r-section" id="evidence">
    <h2>The evidence base is thinner than it looks</h2>

    <p>Keystroke dynamics have emerged as one of the most promising modalities for passive cognitive assessment. Recent studies report impressive numbers:</p>

    <div class="r-stat-row">
      <div class="r-stat">
        <span class="r-stat-number">0.918</span>
        <span class="r-stat-label">AUC for Alzheimer's screening via writing-process biomarkers</span>
        <span class="r-stat-source">Li et al. 2025</span>
      </div>
      <div class="r-stat">
        <span class="r-stat-number">0.997</span>
        <span class="r-stat-label">AUC for MCI discrimination from smartphone keystroke timing</span>
        <span class="r-stat-source">Kim et al. 2024</span>
      </div>
    </div>

    <p>But look closer.</p>

    <div class="r-stat-row">
      <div class="r-stat r-stat-warning">
        <span class="r-stat-number">&lt;500</span>
        <span class="r-stat-label">Total participants across all published keystroke-cognition studies targeting neurodegeneration</span>
      </div>
      <div class="r-stat r-stat-warning">
        <span class="r-stat-number">0</span>
        <span class="r-stat-label">Studies with held-out test sets or external validation cohorts among the highest-reported results</span>
      </div>
    </div>

    <p>Li et al. (2025) computed their AUC on the same 72-person sample used to fit a stepwise logistic regression. Kim et al. (2024) used 99 participants without cross-validation; a correction notice followed. The closest published study to what a modern keystroke-cognition instrument would actually capture (Meulemans, Van Waes, and Leijten 2022) was cross-sectional with n&nbsp;=&nbsp;30.</p>

    <p>The modality is real. The results are suggestive. But the field is drawing field-level conclusions from samples that would not survive a single replication attempt.</p>
  </section>


  <!-- ===== SECTION 2: THE WRONG POPULATION ===== -->
  <section class="r-section" id="population">
    <h2>The studies are on the wrong population</h2>

    <p>Keystroke timing can only reflect cognitive processes when typing is automatic. Below roughly 40 words per minute, variation in flight time is dominated by motor search and planning, not cognitive state. The cognitive signal is buried under motor variance that has nothing to do with neurodegeneration.</p>

    <p>Pinet et al. (2022) documented two qualitatively different processing architectures in typists. In novices, each keystroke requires conscious key location, finger selection, and movement execution. In experts, the motor layer is automated and the keystroke signal becomes transparent to cognitive processes: word retrieval, syntactic planning, coherence monitoring.</p>

    <p>The population currently at risk for cognitive decline (ages 60-80, born 1944-1964) largely did not grow up typing.</p>

    <div class="r-timeline">
      <div class="r-timeline-header">
        <span>Birth cohort</span>
        <span>First regular computer use</span>
        <span>Expected motor noise</span>
      </div>
      <div class="r-timeline-row">
        <span class="r-timeline-label">1940-1955</span>
        <span class="r-timeline-detail">Age 35-55 (1990s)</span>
        <div class="r-bar-container"><div class="r-bar" style="width: 90%"><span class="r-bar-text">High</span></div></div>
      </div>
      <div class="r-timeline-row">
        <span class="r-timeline-label">1955-1965</span>
        <span class="r-timeline-detail">Age 25-40 (1990s)</span>
        <div class="r-bar-container"><div class="r-bar" style="width: 65%"><span class="r-bar-text">Moderate</span></div></div>
      </div>
      <div class="r-timeline-row">
        <span class="r-timeline-label">1965-1980</span>
        <span class="r-timeline-detail">Age 15-30 (1990s)</span>
        <div class="r-bar-container"><div class="r-bar" style="width: 40%"><span class="r-bar-text">Variable</span></div></div>
      </div>
      <div class="r-timeline-row">
        <span class="r-timeline-label">1980-1995</span>
        <span class="r-timeline-detail">Childhood</span>
        <div class="r-bar-container"><div class="r-bar" style="width: 18%"><span class="r-bar-text">Low</span></div></div>
      </div>
      <div class="r-timeline-row">
        <span class="r-timeline-label">1995-2010</span>
        <span class="r-timeline-detail">Birth (native)</span>
        <div class="r-bar-container"><div class="r-bar" style="width: 8%"><span class="r-bar-text">Minimal</span></div></div>
      </div>
    </div>
    <p class="r-caption">Schematic model of generational typing proficiency. Proficiency labels are editorial estimates informed by cohort-level data (Cramer-Petersen et al. 2022, Dhakal et al. 2018, Pew Research 2019), not individually validated assessments.</p>

    <p>The people currently old enough to be at risk for cognitive decline are disproportionately the wrong people to study this way. This creates signal validity problems (motor inexperience masquerades as cognitive slowing), selection bias (fluent elderly typists are unrepresentative), and confounding (typing proficiency correlates with cognitive reserve).</p>
  </section>


  <!-- ===== SECTION 3: THE WINDOW ===== -->
  <section class="r-section" id="window">
    <h2>The window is opening. And closing.</h2>

    <p>The demographic confound is self-resolving. By 2045, the 60-80 age range will consist almost entirely of lifelong fluent typists. The motor noise floor drops with each decade. The automaticity threshold will be met by default.</p>

    <p>But this projection has a load-bearing assumption: that those cohorts are still producing unassisted text character-by-character at volume when they arrive.</p>

    <div class="r-callout">
      <p>The cohort arriving with typing fluency is the same cohort arriving with maximum AI-mediation exposure.</p>
    </div>

    <p>Autocomplete, predictive text, and AI-assisted drafting are not future threats. They are present features of how most people under 40 already write. The demographic shift that would have resolved the motor confound is being partially foreclosed by a technology shift operating on a faster timeline.</p>

    <div class="r-window-diagram">
      <div class="r-window-track">
        <div class="r-window-label-left">Demographic resolution<br/><span class="r-muted">(motor noise declining)</span></div>
        <div class="r-window-label-right">AI contamination<br/><span class="r-muted">(construct replacement rising)</span></div>
        <div class="r-window-gap">
          <span class="r-window-gap-label">The window</span>
        </div>
      </div>
      <div class="r-window-arrow">
        <span>2025</span>
        <span class="r-window-arrow-line"></span>
        <span>2055</span>
      </div>
    </div>

    <p>The cohort born 1980-1995 is the only generation in history that types fluently from childhood, remembers writing without AI assistance, and will enter the cognitive decline screening window (ages 55-75) between 2035 and 2070. If personal baselines are not started on this cohort within the next 10-15 years, the opportunity is permanently lost. There is no second cohort with this combination of properties.</p>
  </section>


  <!-- ===== SECTION 4: CONSTRUCT REPLACEMENT ===== -->
  <section class="r-section" id="construct">
    <h2>AI mediation doesn't add noise. It replaces the construct.</h2>

    <p>This is the argument the field has not yet engaged with.</p>

    <div class="r-comparison">
      <div class="r-comparison-box">
        <h3>Noise</h3>
        <p>The construct being measured is intact. The instrument captures it with some degree of error. More data helps. You can model and correct it.</p>
        <div class="r-comparison-example">A bathroom scale that reads two pounds heavy. The construct (weight) is unchanged.</div>
      </div>
      <div class="r-comparison-box r-comparison-highlight">
        <h3>Construct replacement</h3>
        <p>The measurement now corresponds to a different construct entirely. The surface form looks identical. The generating process is different. More data makes it worse.</p>
        <div class="r-comparison-example">A bathroom scale on a trampoline during an earthquake. The readings are precise. They are precisely wrong.</div>
      </div>
    </div>

    <p>When a person retrieves a word from memory and types it character by character, the timing data reflects lexical retrieval, orthographic encoding, and motor execution. When the same word appears because the person accepted a predictive text suggestion, the surface output is identical. The cognitive process is different: visual scanning, suggestion evaluation, acceptance execution.</p>

    <p>This is empirically documented:</p>

    <div class="r-evidence-stack">
      <div class="r-evidence">
        <span class="r-evidence-finding">Predictive text shifts production from generation to selection, reducing lexical variety independently of vocabulary size.</span>
        <span class="r-evidence-source">Arnold, Chauncey, and Gajos 2020</span>
      </div>
      <div class="r-evidence">
        <span class="r-evidence-finding">Autocomplete shifts pause distributions from lexical-retrieval patterns to suggestion-evaluation patterns.</span>
        <span class="r-evidence-source">Banovic et al. 2019</span>
      </div>
      <div class="r-evidence">
        <span class="r-evidence-finding">Phrase-level suggestions alter content, not just speed. Writers restructure their message to accommodate what the system offers.</span>
        <span class="r-evidence-source">Buschek, Zurn, and Eiber 2021</span>
      </div>
      <div class="r-evidence">
        <span class="r-evidence-finding">AI-assisted writing enhances individual quality while reducing collective diversity. Different writers converge toward similar outputs.</span>
        <span class="r-evidence-source">Doshi and Hauser 2024</span>
      </div>
      <div class="r-evidence r-evidence-key">
        <span class="r-evidence-finding">After seven days of AI-assisted idea generation, creativity dropped and content homogeneity continued climbing even after the AI was removed. The "creative scar" persists beyond the mediation.</span>
        <span class="r-evidence-source">Zhou and Liu 2025</span>
      </div>
    </div>

    <p>The contamination is invisible at the point of collection. No dataset records whether a word was typed or accepted from a suggestion. Detection is impossible post-hoc. The distinguishing information exists only in process-level data that standard behavioral datasets do not collect.</p>
  </section>


  <!-- ===== SECTION 5: THE PRECEDENT ===== -->
  <section class="r-section" id="precedent">
    <h2>This has happened before</h2>

    <p>The clock-drawing test has been a standard neuropsychological screening tool for decades. Draw a clock face showing a specified time. It assesses visuospatial ability, executive function, and semantic memory in a single brief task.</p>

    <div class="r-callout">
      <p>Vishnevsky, Fisher, and Specktor (2024) demonstrated that Gen Z adults underperform on the clock-drawing test not because of cognitive impairment but because of reduced familiarity with analog clock faces.</p>
    </div>

    <p>The assessment embedded a hidden cultural competency that is no longer universal. Any cognitive assessment tied to a specific artifact or input modality has a finite validity window. Keyboards will eventually be displaced. The instruments built on them need to be designed so the cognitive constructs being measured can migrate to new input modalities, not be permanently coupled to keystroke timing.</p>
  </section>


  <!-- ===== SECTION 6: THE PARADIGM SHIFT ===== -->
  <section class="r-section" id="paradigm">
    <h2>From "compared to healthy people" to "compared to yourself"</h2>

    <p>Every published keystroke-cognition study asks: does this person look impaired compared to a population norm? That is the only question available from a one-time lab visit.</p>

    <p>Once you have someone typing fluently every day for years, you can ask a different question:</p>

    <div class="r-callout r-callout-large">
      <p>Does this person look different from themselves?</p>
    </div>

    <div class="r-paradigm-grid">
      <div class="r-paradigm-item">
        <span class="r-paradigm-from">Cross-sectional</span>
        <span class="r-paradigm-arrow"></span>
        <span class="r-paradigm-to">Longitudinal</span>
        <span class="r-paradigm-detail">Thousands of data points per person over years, not a single snapshot.</span>
      </div>
      <div class="r-paradigm-item">
        <span class="r-paradigm-from">Population-normed</span>
        <span class="r-paradigm-arrow"></span>
        <span class="r-paradigm-to">Self-referential</span>
        <span class="r-paradigm-detail">Comparison to your own history, not a healthy average. Eliminates confounds of education, occupation, and baseline ability.</span>
      </div>
      <div class="r-paradigm-item">
        <span class="r-paradigm-from">Task-constrained</span>
        <span class="r-paradigm-arrow"></span>
        <span class="r-paradigm-to">Ecologically valid</span>
        <span class="r-paradigm-detail">Data from naturalistic daily writing, not artificial lab tasks.</span>
      </div>
      <div class="r-paradigm-item">
        <span class="r-paradigm-from">Diagnosis</span>
        <span class="r-paradigm-arrow"></span>
        <span class="r-paradigm-to">Early detection</span>
        <span class="r-paradigm-detail">"Your trajectory shifted six months ago" rather than "you score below the cutoff today."</span>
      </div>
    </div>

    <p>This shift is not novel. It has a 30-year research tradition. Ecological Momentary Assessment (Shiffman, Stone, and Hufford 2008), Experience Sampling Methods (Csikszentmihalyi and Larson 1987), and idiographic approaches to behavioral science (Molenaar 2004) all argue that group-level averages obscure the individual-level dynamics that matter clinically. Molenaar's manifesto demonstrated formally that inter-individual variation and intra-individual variation are statistically non-ergodic: what is true of a population average is not necessarily true of any individual in that population. N-of-1 trial designs are now accepted by the FDA for rare disease contexts.</p>

    <p>What these traditions lack is a measurement instrument that can sustain daily ecological data collection over years in healthy populations. EMA studies typically last days to weeks. ESM relies on self-report prompted by random signals. Neither captures behavioral process data (keystrokes, pause architecture) at the temporal resolution needed for cognitive signal extraction. The paradigm exists. The instrument does not.</p>
  </section>


  <!-- ===== SECTION 7: THE INSTRUMENT GAP ===== -->
  <section class="r-section" id="gap">
    <h2>The instrument gap</h2>

    <p>The current research landscape is fragmented into two silos that do not talk to each other.</p>

    <div class="r-silos">
      <div class="r-silo">
        <h3>Keystroke dynamics researchers</h3>
        <p>Capture timing data but ignore the content of what is typed. Can tell you flight time is elevated but not whether the writer was struggling to retrieve a specific word or planning a complex sentence.</p>
      </div>
      <div class="r-silo-gap">
        <span class="r-silo-gap-symbol">&times;</span>
      </div>
      <div class="r-silo">
        <h3>Computational linguistics researchers</h3>
        <p>Analyze transcribed text but have no access to the temporal process that produced it. Can tell you vocabulary diversity is declining but not whether the decline reflects retrieval difficulty or avoidance.</p>
      </div>
    </div>

    <p>The combination is not additive. It is multiplicative. A decline in lexical diversity concurrent with a decline in production fluency means something clinically different from a decline in diversity with stable fluency. The first suggests retrieval difficulty under cognitive load. The second suggests vocabulary contraction independent of production. You need both signal channels to distinguish them.</p>

    <p>No existing instrument at validated scale combines:</p>

    <div class="r-requirements">
      <div class="r-requirement">
        <span class="r-req-label">Process capture</span>
        <span class="r-req-detail">Keystroke-level temporal data during the act of writing</span>
      </div>
      <div class="r-requirement">
        <span class="r-req-label">Content analysis</span>
        <span class="r-req-detail">Linguistic features from the submitted text</span>
      </div>
      <div class="r-requirement">
        <span class="r-req-label">Longitudinal architecture</span>
        <span class="r-req-detail">Personal baselines over months and years</span>
      </div>
      <div class="r-requirement">
        <span class="r-req-label">Calibration</span>
        <span class="r-req-detail">Within-person same-day baselines via neutral prompts</span>
      </div>
      <div class="r-requirement">
        <span class="r-req-label">Unmediated input</span>
        <span class="r-req-detail">Writing conditions that prevent construct replacement</span>
      </div>
      <div class="r-requirement">
        <span class="r-req-label">Modality awareness</span>
        <span class="r-req-detail">Cognitive constructs not permanently coupled to keyboards</span>
      </div>
    </div>
  </section>


  <!-- ===== SECTION 8: WHY WRITING ===== -->
  <section class="r-section" id="writing">
    <h2>Why writing is a uniquely rich cognitive channel</h2>

    <p>The preceding sections establish that a longitudinal instrument is needed, that the window for building one is finite, and that no existing instrument fills the gap. But why should the instrument be built on writing? Why not speech, gait analysis, eye tracking, or any other behavioral modality?</p>

    <p>Writing is the only common daily behavior that simultaneously requires lexical retrieval, syntactic planning, coherence monitoring, semantic integration, and fine motor execution. No other modality loads this many cognitive systems at once during a single naturalistic act.</p>

    <div class="r-comparison">
      <div class="r-comparison-box">
        <h3>Speech</h3>
        <p>Rich in prosody and lexical content but lacks the motor encoding channel. Articulatory planning is automated differently from keystroke execution. No character-level timing data. Harder to capture in private, ecologically valid contexts without introducing observer effects.</p>
      </div>
      <div class="r-comparison-box">
        <h3>Gait / Motor</h3>
        <p>Strong evidence for motor biomarkers of neurodegeneration (Buchman and Bennett 2011). But gait captures motor and postural control without linguistic content. It cannot distinguish cognitive slowing from physical deconditioning without a second signal channel.</p>
      </div>
    </div>

    <div class="r-comparison">
      <div class="r-comparison-box">
        <h3>Eye tracking</h3>
        <p>Captures attentional allocation and reading fluency but not production. Measures how someone processes existing text, not how they generate new text. The generative act is where retrieval difficulty, planning breakdown, and coherence failure become visible.</p>
      </div>
      <div class="r-comparison-box">
        <h3>Typed writing</h3>
        <p>Combines fine motor timing (keystroke dynamics), lexical production (word choice, vocabulary), syntactic structure (sentence complexity), semantic coherence (topic management), and temporal process data (pause architecture, revision patterns). All captured simultaneously from a single naturalistic act.</p>
      </div>
    </div>

    <p>Kellogg (1996) and Hayes and Flower (1980) established that written composition recruits working memory, long-term memory retrieval, executive monitoring, and motor execution in a coordinated process. Pinet et al. (2022) demonstrated that in fluent typists, the motor layer becomes transparent to cognitive processes: keystroke timing variations reflect lexical retrieval difficulty, syntactic planning load, and coherence monitoring, not finger search. The typing is automatic. What remains in the signal is cognition.</p>

    <p>Writing also has a property no other modality shares: the act itself is independently valuable. Pennebaker's 200+ studies establish that expressive writing produces measurable psychological and physiological benefits whether or not the output is kept or reviewed. People already write daily, in private, with no expectation of feedback. The measurement modality is also the retention mechanism. The instrument does not need to justify its existence to the participant through data display, because the practice justifies itself.</p>
  </section>


  <!-- ===== SECTION 9: EXISTING INSTRUMENTS ===== -->
  <section class="r-section" id="landscape">
    <h2>Existing instruments and what they measure</h2>

    <p>Several approaches to longitudinal behavioral and cognitive measurement exist. Each captures some of the signal channels an instrument would need. None combines them. The gaps are structural, not accidental.</p>

    <div class="r-landscape-grid">
      <div class="r-landscape-card">
        <h3>AI-assisted journaling</h3>
        <div class="r-landscape-names">Rosebud, Reflection, Life Note, Mindsera</div>
        <div class="r-landscape-has">
          <span class="r-landscape-check">Learns from user history</span>
          <span class="r-landscape-check">Content analysis</span>
        </div>
        <div class="r-landscape-lacks">
          <span class="r-landscape-x">Surfaces analysis back to participant</span>
          <span class="r-landscape-x">No keystroke-level process capture</span>
          <span class="r-landscape-x">Feedback loop contaminates behavioral signal</span>
        </div>
        <p class="r-landscape-failure">Showing the participant what the system has learned creates a reflexive loop: the participant writes about the patterns they were shown, which the system detects as new patterns. The instrument alters the construct it measures.</p>
      </div>

      <div class="r-landscape-card">
        <h3>Self-report mood tracking</h3>
        <div class="r-landscape-names">Daylio, Bearable, How We Feel</div>
        <div class="r-landscape-has">
          <span class="r-landscape-check">Longitudinal architecture</span>
        </div>
        <div class="r-landscape-lacks">
          <span class="r-landscape-x">Self-reported, not behavioral</span>
          <span class="r-landscape-x">No process-level data</span>
          <span class="r-landscape-x">Gamification introduces measurement artifacts</span>
        </div>
        <p class="r-landscape-failure">Captures what the participant reports feeling, not how they are processing. Self-report is subject to recall bias, social desirability effects, and alexithymia. Gamification mechanics (streaks, scores) create engagement patterns that confound the signal.</p>
      </div>

      <div class="r-landscape-card">
        <h3>Physiological wearables</h3>
        <div class="r-landscape-names">WHOOP, Oura, Apple Watch, Exist.io</div>
        <div class="r-landscape-has">
          <span class="r-landscape-check">Dense longitudinal data</span>
          <span class="r-landscape-check">Personal baselines</span>
        </div>
        <div class="r-landscape-lacks">
          <span class="r-landscape-x">Cardiovascular, not cognitive</span>
          <span class="r-landscape-x">Surfaces all data to participant</span>
          <span class="r-landscape-x">No writing-process signals</span>
        </div>
        <p class="r-landscape-failure">Measures autonomic readiness, not cognitive process. Transparent scoring creates optimization behavior: participants modify their behavior to improve the score rather than the underlying state. The instrument changes what it measures (Goodhart's law).</p>
      </div>

      <div class="r-landscape-card">
        <h3>Clinical cognitive batteries</h3>
        <div class="r-landscape-names">MoCA, MMSE, clock-drawing, computerized batteries</div>
        <div class="r-landscape-has">
          <span class="r-landscape-check">Validated constructs</span>
          <span class="r-landscape-check">Clinical utility</span>
        </div>
        <div class="r-landscape-lacks">
          <span class="r-landscape-x">Cross-sectional only</span>
          <span class="r-landscape-x">Task-constrained, not ecological</span>
          <span class="r-landscape-x">Cannot sustain daily administration</span>
        </div>
        <p class="r-landscape-failure">A 45-minute assessment every 12 months cannot detect gradual trajectory shifts. Practice effects accumulate with repeated administration. These instruments diagnose after clinical threshold. They were not designed for early detection of pre-clinical drift.</p>
      </div>

      <div class="r-landscape-card">
        <h3>Digital therapeutics</h3>
        <div class="r-landscape-names">Woebot (discontinued 2025), Wysa, Headspace, Calm</div>
        <div class="r-landscape-has">
          <span class="r-landscape-check">Daily engagement architecture</span>
        </div>
        <div class="r-landscape-lacks">
          <span class="r-landscape-x">Journaling is a bolt-on, not the primary modality</span>
          <span class="r-landscape-x">Scripted interactions, not generative</span>
          <span class="r-landscape-x">No behavioral signal computation</span>
        </div>
        <p class="r-landscape-failure">Woebot had the strongest clinical evidence of any digital mental health tool and ceased operations in June 2025. Scripted content libraries depreciate. They do not accumulate participant-specific longitudinal records.</p>
      </div>

      <div class="r-landscape-card">
        <h3>Ephemeral writing tools</h3>
        <div class="r-landscape-names">Drift, Halka, Presently</div>
        <div class="r-landscape-has">
          <span class="r-landscape-check">No data surfaced to participant</span>
        </div>
        <div class="r-landscape-lacks">
          <span class="r-landscape-x">No learning from accumulated history</span>
          <span class="r-landscape-x">No signal computation</span>
          <span class="r-landscape-x">No data retention</span>
        </div>
        <p class="r-landscape-failure">Closest to addressing the observer-effect problem. Entries fade or vanish, removing the feedback loop. But without data retention, no longitudinal analysis is possible. They solve the reactivity problem by abandoning the longitudinal one.</p>
      </div>
    </div>

    <p>The structural pattern: every approach that accumulates participant data surfaces it back to the participant. Every approach that avoids the observer effect discards the data. No existing instrument combines longitudinal accumulation with participant-blind measurement. The combination is architecturally difficult because the standard model for justifying sustained participant engagement is showing them what you've learned. An instrument that learns but does not show must find a different mechanism for retention.</p>
  </section>


  <!-- ===== SECTION 10: THE OBSERVER EFFECT ===== -->
  <section class="r-section" id="observer">
    <h2>The observer effect is not a design preference. It is a validity requirement.</h2>

    <p>This is the hardest design constraint to defend, and the most important to get right.</p>

    <p>The Hawthorne effect (Adair 1984; McCambridge, Witton, and Elbourne 2014) is well-established: participants who know they are being observed modify their behavior. In longitudinal behavioral measurement, this is not a minor confound. It is fatal to the construct. If a participant knows their writing speed is being measured, they write differently. If they know lexical diversity is being tracked, they reach for unusual words. The signal no longer reflects natural cognitive process. It reflects performance.</p>

    <p>Surfacing computed signals to the participant creates a second, deeper problem: optimization. Goodhart's law applies directly. Once the participant sees a metric, they optimize for the metric rather than the state the metric was designed to detect. Etkin (2016, Journal of Consumer Research) demonstrated across six experiments that personal quantification increases behavioral output but reduces enjoyment and intrinsic motivation. The act of measuring changes the relationship to the measured activity.</p>

    <p>The clinical extreme is documented. Rosman et al. (2021, NIH-funded) reported a patient with atrial fibrillation who performed 916 smartwatch ECGs in one year after the device began surfacing cardiac data. Smartwatch notifications triggered escalating monitoring behavior: ambiguous readings ("inconclusive") produced the same behavioral response as actual arrhythmia detections. The patient developed illness anxiety disorder, made 12 unnecessary clinic and emergency department visits, and required cognitive behavioral therapy for remission. The authors note anecdotal reports from institutions nationwide, suggesting the case represents "the tip of an iceberg."</p>

    <p>In a longitudinal cognitive instrument, this dynamic is not a side effect. It is a confound that accumulates over time. A participant who knows their lexical diversity is being measured reaches for unusual words. A participant who sees a decline in their processing speed metric becomes anxious, and the anxiety alters the very signals being measured. The instrument creates the condition it was designed to detect.</p>

    <p>A longitudinal cognitive instrument must therefore satisfy a paradox: it must accumulate behavioral data over years, learn from that data to improve measurement precision, and never show the participant what it has learned. The participant's relationship to the instrument must be with the surface (the question, the writing practice) while the measurement operates underneath, invisible.</p>

    <p>The problem compounds in clinical populations. Self-censorship is documented across journaling communities and is most acute in populations with PTSD, trauma histories, and anxiety disorders. The people who would produce the most clinically valuable writing data are the most likely to alter their writing when they know it is being analyzed. An instrument that surfaces its analysis guarantees that the participants who need it most will either censor themselves or stop using it.</p>

    <p>This is not paternalism. It is the same logic that prevents a scale from displaying weight during a body composition study, or a blood pressure cuff from showing readings during an ambulatory monitoring protocol. The measurement must not alter the measured state.</p>
  </section>


  <!-- ===== SECTION 11: INSTRUMENT REQUIREMENTS ===== -->
  <section class="r-section" id="design">
    <h2>Requirements for instrument validity</h2>

    <p>The preceding analysis constrains the design. A valid instrument for longitudinal cognitive measurement through naturalistic writing must satisfy all of the following simultaneously. Satisfying five of six produces a flawed instrument, not a slightly less good one.</p>

    <p>A question researchers will ask immediately: why build this as a product people choose to use, rather than a research tool distributed through clinics or universities? Because the sixth requirement demands it. Longitudinal studies that rely on institutional recruitment and scheduled lab visits cannot sustain daily participation over years. Retention in traditional EMA studies drops sharply after days to weeks. An instrument that produces valid longitudinal data must be something people want to use for its own sake, in their own lives, without researcher contact. Ecological validity and sustained retention are not product decisions. They are methodological requirements that happen to produce something that looks like a product.</p>

    <div class="r-design-list">
      <div class="r-design-item">
        <span class="r-design-constraint">Ecological validity</span>
        <span class="r-design-solution">The writing task must be intrinsically meaningful, not a lab exercise. Participants must want to do it for its own sake, daily, for years. A task perceived as "testing" triggers performance behavior and contaminates the signal.</span>
      </div>
      <div class="r-design-item">
        <span class="r-design-constraint">Unmediated input</span>
        <span class="r-design-solution">No autocomplete, predictive text, or AI assistance during the writing task. Any mediation replaces the cognitive construct being measured (Section 4). The input environment must guarantee that every character reflects a discrete motor and cognitive act.</span>
      </div>
      <div class="r-design-item">
        <span class="r-design-constraint">Dual-channel capture</span>
        <span class="r-design-solution">Both keystroke-level temporal data (process) and submitted text (content) must be captured and analyzed jointly. Neither channel alone can distinguish retrieval difficulty from vocabulary contraction, or cognitive slowing from motor fatigue (Section 7).</span>
      </div>
      <div class="r-design-item">
        <span class="r-design-constraint">Intra-individual baselines</span>
        <span class="r-design-solution">Deviation must be measured against the participant's own history, not a population norm. Calibration sessions provide within-person same-day controls that account for transient state variation (fatigue, illness, mood).</span>
      </div>
      <div class="r-design-item">
        <span class="r-design-constraint">Participant-blind measurement</span>
        <span class="r-design-solution">Computed signals must never be surfaced to the participant. Awareness of measurement alters the measured behavior (Hawthorne). Awareness of specific metrics triggers optimization (Goodhart). The instrument must learn from accumulated data without creating a feedback loop.</span>
      </div>
      <div class="r-design-item">
        <span class="r-design-constraint">Retention without gamification</span>
        <span class="r-design-solution">Sustained daily engagement over years cannot rely on streaks, scores, or dashboards, as these are measurement artifacts. The instrument must generate sufficient intrinsic value through the practice itself. Improving question quality from accumulated response history is one mechanism.</span>
      </div>
    </div>
  </section>


  <!-- ===== SECTION 12: THEORETICAL EXTENSIONS ===== -->
  <section class="r-section" id="extensions">
    <h2>Theoretical extensions (working frameworks, not published findings)</h2>

    <p>The argument on this page addresses one modality (typed writing) and one measurement context (longitudinal cognitive assessment). Two broader frameworks are in development. Both are working papers, not peer-reviewed. They are included here to indicate where this thinking leads, not to claim results.</p>

    <h3 class="r-subsection-heading">Construct replacement is modality-general</h3>

    <p>The construct replacement problem described in Section 4 is not specific to typing. AI mediation is simultaneously altering the cognitive processes underlying speech (voice assistants restructure how people formulate requests), spatial navigation (GPS replaces cognitive map formation), decision-making (recommendation engines replace evaluative reasoning), and writing. Each modality has its own research silo documenting the effects. None has named the common structure.</p>

    <p>A theoretical framework paper in development argues that AI-mediated behavioral change is a modality-general problem requiring modality-general design constraints. Four constraints emerge consistently across modalities: unassisted input, process-level capture (temporal microstructure, not aggregate performance), longitudinal intra-individual baselines, and attachment to intrinsically motivated practices. The six instrument requirements derived on this page are a domain-specific instantiation of these four general constraints. The convergence suggests the problem and the solution are both deeper than any single behavioral channel.</p>

    <h3 class="r-subsection-heading">The process record</h3>

    <p>The deepest version of this argument is not scientific but philosophical. Three levels of record exist for any cognitive act: the artifact (what was produced), the genome (the hardware blueprint of the producer), and the process record (the temporal trace of a mind producing the artifact in real time). The process record is asymmetrically more fundamental than the artifact: you can derive the artifact from the process, but not the process from the artifact. A finished essay contains none of the pauses, revisions, retrieval failures, and planning that produced it. The keystroke stream contains all of them.</p>

    <p>AI mediation does not contaminate the process record. It replaces it. When a sentence is accepted from an autocomplete suggestion, no retrieval occurred, no syntactic planning happened, no coherence monitoring was needed. The process record for that sentence is not noisy. It is absent. We are in the first generation where the conditions for capturing unmediated process records at scale are becoming permanently unavailable. The closing window described on this page is, at its deepest level, a window for preserving a category of data about biological intelligence that will not be producible once the mediation is universal.</p>

    <p>This is a speculative framework. It is included because it clarifies why the practical instrument design described above is not just a useful research tool but a response to an irreversible epistemic loss.</p>
  </section>


  <!-- ===== SECTION 13: OPEN QUESTIONS ===== -->
  <section class="r-section" id="open">
    <h2>Open questions and limitations</h2>

    <p>Alice is a prototype implementation of this instrument design, accumulating data at n=1 since April 2026. It satisfies the six requirements architecturally. Whether it satisfies them in practice is an empirical question that cannot be answered at current scale. The following problems remain open.</p>

    <div class="r-design-list">
      <div class="r-design-item">
        <span class="r-design-constraint">Validation</span>
        <span class="r-design-solution">No longitudinal keystroke-cognition study with validated outcomes exists at any sample size. The instrument is designed to produce the data that would enable such a study. The validation must come from the data, not precede it.</span>
      </div>
      <div class="r-design-item">
        <span class="r-design-constraint">Signal-to-construct mapping</span>
        <span class="r-design-solution">The mapping between keystroke timing features and cognitive constructs (lexical retrieval, executive function, processing speed) is hypothesized from the psycholinguistic literature, not validated in a longitudinal context. These mappings are candidates, not established biomarkers.</span>
      </div>
      <div class="r-design-item">
        <span class="r-design-constraint">Retention</span>
        <span class="r-design-solution">Whether question quality from accumulated history is sufficient to sustain daily engagement over years without gamification is unknown. n=1 cannot answer this. The mechanism is theoretically grounded (personalization increases perceived value) but empirically untested at scale.</span>
      </div>
      <div class="r-design-item">
        <span class="r-design-constraint">Modality migration</span>
        <span class="r-design-solution">If keyboard input is displaced by voice, gesture, or neural interfaces, the keystroke-specific signals become invalid. The cognitive constructs must be defined independently of the input modality so measurement can migrate. This is a design goal, not a solved problem.</span>
      </div>
      <div class="r-design-item">
        <span class="r-design-constraint">Ethics of silent measurement</span>
        <span class="r-design-solution">Participant-blind measurement requires informed consent about the existence of measurement without disclosure of specific metrics. The ethical framework for this exists in ambulatory monitoring research but has not been applied to keystroke-level cognitive assessment. The boundaries need explicit articulation.</span>
      </div>
    </div>

    <p>The baselines need to be accumulating now, so that when the validation studies become possible, the longitudinal records already exist. The gap between "implemented" and "validated" is the work that remains.</p>

    <div class="r-close-links">
      <a href="/papers">Read the papers</a>
      <a href="/landing">See the prototype</a>
    </div>
  </section>


  <!-- ===== SOURCES ===== -->
  <footer class="r-sources">
    <h2>Sources</h2>
    <p>This page draws from two preprints and the primary literature they cite. Full reference lists are in the source papers.</p>
    <ul>
      <li>Guzzardo, A. (2026). "A Closing Window: The Demographic Confound in Keystroke-Based Cognitive Biomarkers and the AI-Mediation Threat to the Paradigm That Would Replace It." Preprint, v2.</li>
      <li>Guzzardo, A. (2026). "Construct Replacement: When AI-Mediated Input Invalidates Behavioral Measurement." Preprint, v2.</li>
    </ul>
    <p class="r-sources-note">Key citations: Adair 1984; Arnold et al. 2020; Banovic et al. 2019; Buchman &amp; Bennett 2011; Buschek et al. 2021; Covington &amp; McFall 2010; Csikszentmihalyi &amp; Larson 1987; Dhakal et al. 2018; Doshi &amp; Hauser 2024; Etkin 2016; Hayes &amp; Flower 1980; Kellogg 1996; Kim et al. 2024; Li et al. 2025; McCambridge, Witton &amp; Elbourne 2014; Meulemans et al. 2022; Molenaar 2004; Pennebaker 1997; Pinet et al. 2022; Rosman et al. 2021; Shiffman, Stone &amp; Hufford 2008; Vishnevsky et al. 2024; Yamaguchi &amp; Logan 2014; Zhou &amp; Liu 2025.</p>
  </footer>

  </article>

</div><!-- .r-layout -->

</PublicLayout>

<script>
  // Scroll-spy: highlight the active nav link based on scroll position
  document.addEventListener('DOMContentLoaded', () => {
    const nav = document.querySelector('.r-nav');
    if (!nav) return;

    const links = nav.querySelectorAll('.r-nav-link');
    const sections = Array.from(links).map(link => {
      const id = link.getAttribute('data-section');
      return id ? document.getElementById(id) : null;
    }).filter(Boolean) as HTMLElement[];

    const observer = new IntersectionObserver((entries) => {
      for (const entry of entries) {
        if (entry.isIntersecting) {
          const id = entry.target.id;
          links.forEach(link => {
            link.classList.toggle('active', link.getAttribute('data-section') === id);
          });
        }
      }
    }, { rootMargin: '-20% 0px -60% 0px' });

    sections.forEach(s => observer.observe(s));
  });

  // "Closing window" visualization: two curves converging
  // Left curve: motor noise declining (demographic resolution)
  // Right curve: AI contamination rising (construct replacement)
  // The gap between them is the window.
  (() => {
    const canvas = document.getElementById('window-viz') as HTMLCanvasElement;
    if (!canvas) return;
    const ctx = canvas.getContext('2d');
    if (!ctx) return;

    const dpr = window.devicePixelRatio || 1;
    const w = canvas.width;
    const h = canvas.height;
    canvas.width = w * dpr;
    canvas.height = h * dpr;
    canvas.style.width = w + 'px';
    canvas.style.height = h + 'px';
    ctx.scale(dpr, dpr);

    // Read CSS variables from computed style
    const style = getComputedStyle(document.documentElement);
    const textFaint = style.getPropertyValue('--text-faint').trim() || '#999';
    const border = style.getPropertyValue('--border').trim() || '#ddd';
    const accent = style.getPropertyValue('--accent').trim() || '#8b7355';
    const negative = style.getPropertyValue('--negative').trim() || '#c44';

    const pad = { top: 40, right: 30, bottom: 50, left: 30 };
    const plotW = w - pad.left - pad.right;
    const plotH = h - pad.top - pad.bottom;

    // Axes
    ctx.strokeStyle = border;
    ctx.lineWidth = 1;
    ctx.beginPath();
    ctx.moveTo(pad.left, pad.top);
    ctx.lineTo(pad.left, pad.top + plotH);
    ctx.lineTo(pad.left + plotW, pad.top + plotH);
    ctx.stroke();

    // Year labels
    ctx.fillStyle = textFaint;
    ctx.font = '10px monospace';
    ctx.textAlign = 'center';
    const years = [2025, 2030, 2035, 2040, 2045, 2050, 2055];
    for (const yr of years) {
      const x = pad.left + ((yr - 2025) / 30) * plotW;
      ctx.fillText(String(yr), x, pad.top + plotH + 18);

      // Tick
      ctx.strokeStyle = border;
      ctx.globalAlpha = 0.3;
      ctx.beginPath();
      ctx.moveTo(x, pad.top);
      ctx.lineTo(x, pad.top + plotH);
      ctx.stroke();
      ctx.globalAlpha = 1;
    }

    // Motor noise curve (declining sigmoid)
    const motorNoise = (t: number) => 0.92 / (1 + Math.exp(4 * (t - 0.45))) + 0.05;
    // AI contamination curve (rising sigmoid)
    const aiContam = (t: number) => 0.88 / (1 + Math.exp(-5 * (t - 0.35))) + 0.08;

    const steps = 200;

    // Fill the gap between curves where motor < AI (the window region)
    // Find where they cross
    let crossT = 0;
    for (let i = 0; i <= steps; i++) {
      const t = i / steps;
      if (motorNoise(t) < aiContam(t)) { crossT = t; break; }
    }

    // Shade the window region (where motor is still above AI)
    ctx.fillStyle = accent;
    ctx.globalAlpha = 0.06;
    ctx.beginPath();
    for (let i = 0; i <= steps; i++) {
      const t = i / steps;
      if (t > crossT) break;
      const x = pad.left + t * plotW;
      const y = pad.top + (1 - motorNoise(t)) * plotH;
      if (i === 0) ctx.moveTo(x, y); else ctx.lineTo(x, y);
    }
    // Walk back along AI curve
    for (let i = Math.floor(crossT * steps); i >= 0; i--) {
      const t = i / steps;
      const x = pad.left + t * plotW;
      const y = pad.top + (1 - aiContam(t)) * plotH;
      ctx.lineTo(x, y);
    }
    ctx.closePath();
    ctx.fill();
    ctx.globalAlpha = 1;

    // Draw motor noise curve
    ctx.strokeStyle = textFaint;
    ctx.lineWidth = 2;
    ctx.beginPath();
    for (let i = 0; i <= steps; i++) {
      const t = i / steps;
      const x = pad.left + t * plotW;
      const y = pad.top + (1 - motorNoise(t)) * plotH;
      if (i === 0) ctx.moveTo(x, y); else ctx.lineTo(x, y);
    }
    ctx.stroke();

    // Draw AI contamination curve
    ctx.strokeStyle = negative;
    ctx.lineWidth = 2;
    ctx.beginPath();
    for (let i = 0; i <= steps; i++) {
      const t = i / steps;
      const x = pad.left + t * plotW;
      const y = pad.top + (1 - aiContam(t)) * plotH;
      if (i === 0) ctx.moveTo(x, y); else ctx.lineTo(x, y);
    }
    ctx.stroke();

    // Cross point marker
    const crossX = pad.left + crossT * plotW;
    const crossY = pad.top + (1 - motorNoise(crossT)) * plotH;
    ctx.fillStyle = accent;
    ctx.beginPath();
    ctx.arc(crossX, crossY, 4, 0, Math.PI * 2);
    ctx.fill();

    // "The window" label with dashed bracket
    const windowMidT = crossT * 0.55;
    const windowMidX = pad.left + windowMidT * plotW;
    const motorMidY = pad.top + (1 - motorNoise(windowMidT)) * plotH;
    const aiMidY = pad.top + (1 - aiContam(windowMidT)) * plotH;
    const gapMidY = (motorMidY + aiMidY) / 2;

    ctx.fillStyle = accent;
    ctx.font = 'italic 13px serif';
    ctx.textAlign = 'center';
    ctx.fillText('the window', windowMidX, gapMidY + 4);

    // Curve labels
    ctx.font = '10px monospace';
    ctx.textAlign = 'left';
    ctx.fillStyle = textFaint;
    ctx.fillText('MOTOR NOISE', pad.left + plotW * 0.68, pad.top + (1 - motorNoise(0.7)) * plotH - 8);

    ctx.fillStyle = negative;
    ctx.fillText('AI CONTAMINATION', pad.left + plotW * 0.55, pad.top + (1 - aiContam(0.7)) * plotH + 18);

    // Y-axis label
    ctx.save();
    ctx.fillStyle = textFaint;
    ctx.font = '9px monospace';
    ctx.textAlign = 'center';
    ctx.translate(12, pad.top + plotH / 2);
    ctx.rotate(-Math.PI / 2);
    ctx.fillText('SIGNAL VALIDITY', 0, 0);
    ctx.restore();
  })();
</script>

<style is:global>
  /* ===== Research page styles ===== */

  /* --- Layout: hero full-width, then sidebar + content --- */
  .r-hero {
    display: grid;
    grid-template-columns: 1fr 480px;
    gap: 3rem;
    align-items: center;
    padding: 5rem 0 3rem;
  }

  .r-hero-text h1 {
    font-family: var(--serif-display);
    font-size: 2.2rem;
    font-weight: 400;
    line-height: 1.35;
    color: var(--text);
    margin-bottom: 1.5rem;
  }

  .r-subtitle {
    font-family: var(--serif-body);
    font-size: 1.1rem;
    line-height: 1.7;
    color: var(--text);
  }

  /* --- Hero visualization --- */
  .r-hero-viz {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .r-hero-viz canvas {
    width: 100%;
    height: auto;
    border-radius: 6px;
  }

  .r-hero-viz-caption {
    display: flex;
    justify-content: space-between;
    font-family: var(--mono);
    font-size: 0.65rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--text-faint);
  }

  .r-layout {
    display: grid;
    grid-template-columns: 200px 1fr;
    gap: 4rem;
    align-items: start;
  }

  /* --- Sidebar nav --- */
  .r-nav {
    position: sticky;
    top: 6rem;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    padding-top: 3rem;
  }

  .r-nav-group {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
  }

  .r-nav-group-label {
    font-family: var(--mono);
    font-size: 0.65rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--text-faint);
    padding-bottom: 0.35rem;
    margin-bottom: 0.15rem;
  }

  .r-nav-link {
    font-family: var(--serif-body);
    font-size: 0.85rem;
    color: var(--text-muted);
    text-decoration: none;
    padding: 0.2rem 0 0.2rem 0.75rem;
    border-left: 1px solid transparent;
    transition: color 0.2s ease, border-color 0.2s ease;
    line-height: 1.4;
  }

  .r-nav-link:hover {
    color: var(--text);
    text-decoration: none;
  }

  .r-nav-link.active {
    color: var(--text);
    border-left-color: var(--accent);
  }

  /* --- Content area --- */
  .research {
    padding-bottom: 6rem;
    min-width: 0;
  }

  /* --- Sections --- */
  .r-section {
    padding: 3rem 0;
    border-top: 1px solid var(--border);
    scroll-margin-top: 5rem;
  }

  .r-section h2 {
    font-family: var(--serif-display);
    font-size: 1.5rem;
    font-weight: 400;
    color: var(--text);
    margin-bottom: 1.5rem;
  }

  .r-section p {
    font-family: var(--serif-body);
    font-size: 1.05rem;
    line-height: 1.75;
    color: var(--text);
    margin-bottom: 1.25rem;
    max-width: 720px;
  }

  .r-subsection-heading {
    font-family: var(--mono);
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--text);
    margin-top: 2.5rem;
    margin-bottom: 1rem;
  }

  .r-caption {
    font-size: 0.85rem !important;
    color: var(--text-faint) !important;
    font-style: italic;
    margin-top: -0.5rem;
  }

  .r-muted {
    color: var(--text-muted);
    font-size: 0.85rem;
  }

  /* --- Stat cards --- */
  .r-stat-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    margin: 2rem 0;
  }

  .r-stat {
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .r-stat-number {
    font-family: var(--mono);
    font-size: 2.2rem;
    font-weight: 400;
    color: var(--text);
    letter-spacing: -0.02em;
  }

  .r-stat-label {
    font-family: var(--serif-body);
    font-size: 0.9rem;
    line-height: 1.5;
    color: var(--text);
  }

  .r-stat-source {
    font-family: var(--mono);
    font-size: 0.75rem;
    color: var(--text-muted);
    letter-spacing: 0.02em;
  }

  .r-stat-warning {
    border-color: var(--negative);
  }

  .r-stat-warning .r-stat-number {
    color: var(--negative);
  }

  /* --- Bar chart (timeline) --- */
  .r-timeline {
    margin: 2rem 0;
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 1.5rem;
  }

  .r-timeline-header {
    display: grid;
    grid-template-columns: 100px 140px 1fr;
    gap: 1rem;
    padding-bottom: 0.75rem;
    border-bottom: 1px solid var(--border);
    margin-bottom: 0.75rem;
    font-family: var(--mono);
    font-size: 0.7rem;
    color: var(--text-faint);
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .r-timeline-row {
    display: grid;
    grid-template-columns: 100px 140px 1fr;
    gap: 1rem;
    align-items: center;
    padding: 0.5rem 0;
  }

  .r-timeline-label {
    font-family: var(--mono);
    font-size: 0.85rem;
    color: var(--text);
  }

  .r-timeline-detail {
    font-family: var(--serif-body);
    font-size: 0.85rem;
    color: var(--text);
  }

  .r-bar-container {
    height: 24px;
    background: var(--bg-dim);
    border-radius: 3px;
    overflow: hidden;
  }

  .r-bar {
    height: 100%;
    background: var(--negative);
    opacity: 0.6;
    border-radius: 3px;
    display: flex;
    align-items: center;
    padding-left: 0.5rem;
    min-width: 50px;
  }

  .r-bar-text {
    font-family: var(--mono);
    font-size: 0.7rem;
    color: var(--bg);
    letter-spacing: 0.04em;
    white-space: nowrap;
  }

  /* --- Callout --- */
  .r-callout {
    border-left: 2px solid var(--accent);
    padding: 1.25rem 1.5rem;
    margin: 2rem 0;
    background: var(--bg-dim);
    border-radius: 0 6px 6px 0;
  }

  .r-callout p {
    font-family: var(--serif-display);
    font-size: 1.2rem;
    font-weight: 400;
    color: var(--text);
    margin-bottom: 0;
    line-height: 1.5;
  }

  .r-callout-large p {
    font-size: 1.4rem;
  }

  /* --- Window diagram --- */
  .r-window-diagram {
    margin: 2.5rem 0;
    padding: 2rem;
    border: 1px solid var(--text-muted);
    border-radius: 6px;
  }

  .r-window-track {
    position: relative;
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 1.5rem;
    min-height: 80px;
  }

  .r-window-label-left,
  .r-window-label-right {
    font-family: var(--mono);
    font-size: 0.75rem;
    color: var(--text);
    letter-spacing: 0.04em;
    text-transform: uppercase;
    width: 35%;
    line-height: 1.5;
  }

  .r-window-label-left .r-muted,
  .r-window-label-right .r-muted {
    color: var(--text-muted);
  }

  .r-window-label-right {
    text-align: right;
  }

  .r-window-gap {
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    border: 2px dashed var(--text);
    border-radius: 4px;
    padding: 0.5rem 1.5rem;
    text-align: center;
  }

  .r-window-gap-label {
    font-family: var(--serif-display);
    font-size: 1.1rem;
    font-style: italic;
    color: var(--text);
  }

  .r-window-arrow {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-family: var(--mono);
    font-size: 0.75rem;
    color: var(--text);
  }

  .r-window-arrow-line {
    flex: 1;
    height: 2px;
    background: linear-gradient(to right, var(--positive), var(--text-muted), var(--negative));
  }

  /* --- Comparison boxes (noise vs replacement) --- */
  .r-comparison {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    margin: 2rem 0;
  }

  .r-comparison-box {
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 1.5rem;
  }

  .r-comparison-box h3 {
    font-family: var(--mono);
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-muted);
    margin-bottom: 0.75rem;
  }

  .r-comparison-box p {
    font-family: var(--serif-body);
    font-size: 0.95rem;
    line-height: 1.6;
    color: var(--text);
    margin-bottom: 0.75rem;
  }

  .r-comparison-highlight {
    border-color: var(--negative);
    background: var(--bg-dim);
  }

  .r-comparison-example {
    font-family: var(--serif-body);
    font-size: 0.85rem;
    font-style: italic;
    color: var(--text-muted);
    padding-top: 0.75rem;
    border-top: 1px solid var(--border);
  }

  /* --- Evidence stack --- */
  .r-evidence-stack {
    margin: 2rem 0;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .r-evidence {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    padding: 1rem 1.25rem;
    border-left: 2px solid var(--border);
  }

  .r-evidence-key {
    border-left-color: var(--accent);
    background: var(--bg-dim);
    border-radius: 0 4px 4px 0;
  }

  .r-evidence-finding {
    font-family: var(--serif-body);
    font-size: 0.95rem;
    line-height: 1.6;
    color: var(--text);
  }

  .r-evidence-source {
    font-family: var(--mono);
    font-size: 0.7rem;
    color: var(--text-muted);
    letter-spacing: 0.04em;
  }

  /* --- Paradigm shift grid --- */
  .r-paradigm-grid {
    display: flex;
    flex-direction: column;
    gap: 0;
    margin: 2rem 0;
    border: 1px solid var(--border);
    border-radius: 6px;
    overflow: hidden;
  }

  .r-paradigm-item {
    display: grid;
    grid-template-columns: 140px 24px 140px 1fr;
    gap: 0.75rem;
    align-items: center;
    padding: 1rem 1.25rem;
    border-bottom: 1px solid var(--border);
  }

  .r-paradigm-item:last-child {
    border-bottom: none;
  }

  .r-paradigm-from {
    font-family: var(--mono);
    font-size: 0.75rem;
    color: var(--text-muted);
    text-align: right;
    letter-spacing: 0.02em;
  }

  .r-paradigm-arrow {
    width: 24px;
    height: 1px;
    background: var(--accent);
    position: relative;
  }

  .r-paradigm-arrow::after {
    content: '';
    position: absolute;
    right: 0;
    top: -3px;
    border: 3px solid transparent;
    border-left-color: var(--accent);
  }

  .r-paradigm-to {
    font-family: var(--mono);
    font-size: 0.75rem;
    color: var(--text);
    letter-spacing: 0.02em;
    font-weight: 500;
  }

  .r-paradigm-detail {
    font-family: var(--serif-body);
    font-size: 0.85rem;
    color: var(--text);
    line-height: 1.5;
  }

  /* --- Silos diagram --- */
  .r-silos {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: 1rem;
    align-items: center;
    margin: 2rem 0;
  }

  .r-silo {
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 1.5rem;
  }

  .r-silo h3 {
    font-family: var(--mono);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--text);
    margin-bottom: 0.75rem;
  }

  .r-silo p {
    font-family: var(--serif-body);
    font-size: 0.9rem;
    line-height: 1.6;
    color: var(--text);
    margin-bottom: 0;
  }

  .r-silo-gap {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .r-silo-gap-symbol {
    font-size: 1.5rem;
    color: var(--negative);
    opacity: 0.6;
  }

  /* --- Requirements list --- */
  .r-requirements {
    margin: 2rem 0;
    display: flex;
    flex-direction: column;
    gap: 0;
    border: 1px solid var(--border);
    border-radius: 6px;
    overflow: hidden;
  }

  .r-requirement {
    display: grid;
    grid-template-columns: 180px 1fr;
    gap: 1rem;
    padding: 0.85rem 1.25rem;
    border-bottom: 1px solid var(--border);
    align-items: baseline;
  }

  .r-requirement:last-child {
    border-bottom: none;
  }

  .r-req-label {
    font-family: var(--mono);
    font-size: 0.75rem;
    color: var(--text);
    letter-spacing: 0.04em;
  }

  .r-req-detail {
    font-family: var(--serif-body);
    font-size: 0.9rem;
    color: var(--text);
    line-height: 1.5;
  }

  /* --- Landscape cards --- */
  .r-landscape-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.25rem;
    margin: 2rem 0;
  }

  .r-landscape-card {
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .r-landscape-card h3 {
    font-family: var(--mono);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--text);
    margin: 0;
  }

  .r-landscape-names {
    font-family: var(--serif-body);
    font-size: 0.8rem;
    color: var(--text-muted);
    font-style: italic;
  }

  .r-landscape-has,
  .r-landscape-lacks {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }

  .r-landscape-check,
  .r-landscape-x {
    font-family: var(--serif-body);
    font-size: 0.85rem;
    line-height: 1.4;
    padding-left: 1.25rem;
    position: relative;
  }

  .r-landscape-check {
    color: var(--text);
  }

  .r-landscape-check::before {
    content: '+';
    position: absolute;
    left: 0;
    font-family: var(--mono);
    font-size: 0.85rem;
    color: var(--positive);
    font-weight: 600;
  }

  .r-landscape-x {
    color: var(--text);
  }

  .r-landscape-x::before {
    content: '\2013';
    position: absolute;
    left: 0;
    font-family: var(--mono);
    font-size: 0.85rem;
    color: var(--negative);
  }

  .r-landscape-failure {
    font-family: var(--serif-body) !important;
    font-size: 0.85rem !important;
    line-height: 1.5 !important;
    color: var(--text-muted) !important;
    margin: 0 !important;
    padding-top: 0.5rem;
    border-top: 1px solid var(--border);
  }

  /* --- Design constraints list --- */
  .r-design-list {
    margin: 2rem 0;
    display: flex;
    flex-direction: column;
    gap: 0;
    border: 1px solid var(--border);
    border-radius: 6px;
    overflow: hidden;
  }

  .r-design-item {
    display: grid;
    grid-template-columns: 180px 1fr;
    gap: 1rem;
    padding: 1rem 1.25rem;
    border-bottom: 1px solid var(--border);
    align-items: baseline;
  }

  .r-design-item:last-child {
    border-bottom: none;
  }

  .r-design-constraint {
    font-family: var(--mono);
    font-size: 0.75rem;
    color: var(--text);
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .r-design-solution {
    font-family: var(--serif-body);
    font-size: 0.95rem;
    color: var(--text);
    line-height: 1.6;
  }

  /* --- Close --- */
  .r-close {
    border-top: none;
    padding-top: 1rem;
  }

  .r-close p {
    font-family: var(--serif-display);
    font-size: 1.15rem;
    line-height: 1.6;
    color: var(--text);
  }

  .r-close-links {
    display: flex;
    gap: 2rem;
    margin-top: 2rem;
  }

  .r-close-links a {
    font-family: var(--serif-body);
    font-size: 1.05rem;
    color: var(--accent);
    text-decoration: none;
    border-bottom: 1px solid var(--border);
    padding-bottom: 0.15rem;
    transition: border-color 0.2s ease;
  }

  .r-close-links a:hover {
    border-color: var(--accent);
    text-decoration: none;
  }

  /* --- Sources footer --- */
  .r-sources {
    padding: 3rem 0 2rem;
    border-top: 1px solid var(--border);
    margin-top: 2rem;
  }

  .r-sources h2 {
    font-family: var(--mono);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-faint);
    margin-bottom: 1rem;
  }

  .r-sources p {
    font-family: var(--serif-body);
    font-size: 0.9rem;
    line-height: 1.6;
    color: var(--text);
    margin-bottom: 0.75rem;
  }

  .r-sources ul {
    list-style: none;
    padding: 0;
    margin: 0.75rem 0 1.25rem;
  }

  .r-sources li {
    font-family: var(--serif-body);
    font-size: 0.85rem;
    line-height: 1.6;
    color: var(--text);
    padding: 0.4rem 0;
    padding-left: 1rem;
    border-left: 1px solid var(--border);
  }

  .r-sources-note {
    font-size: 0.8rem !important;
    color: var(--text-faint) !important;
  }

  /* --- Tablet --- */
  @media (max-width: 1024px) {
    .r-layout {
      grid-template-columns: 180px 1fr;
      gap: 2.5rem;
    }
    .r-hero {
      grid-template-columns: 1fr;
    }
    .r-hero-viz canvas {
      max-width: 480px;
    }
  }

  /* --- Mobile --- */
  @media (max-width: 768px) {
    .r-hero { padding: 4rem 0 2.5rem; }
    .r-hero-text h1 { font-size: 1.6rem; }

    .r-layout {
      display: block;
    }

    .r-nav {
      position: static;
      flex-direction: row;
      flex-wrap: wrap;
      gap: 0;
      padding: 1.5rem 0;
      border-bottom: 1px solid var(--border);
      margin-bottom: 1rem;
    }

    .r-nav-group {
      flex-direction: row;
      flex-wrap: wrap;
      gap: 0;
      width: 100%;
      margin-bottom: 0.75rem;
    }

    .r-nav-group-label {
      width: 100%;
      padding-bottom: 0.25rem;
      margin-bottom: 0.25rem;
    }

    .r-nav-link {
      border-left: none;
      padding: 0.25rem 0.75rem;
      font-size: 0.8rem;
    }

    .r-stat-row { grid-template-columns: 1fr; }
    .r-comparison { grid-template-columns: 1fr; }
    .r-landscape-grid { grid-template-columns: 1fr; }

    .r-silos { grid-template-columns: 1fr; }
    .r-silo-gap { padding: 0.5rem 0; }

    .r-paradigm-item {
      grid-template-columns: 1fr;
      gap: 0.25rem;
    }
    .r-paradigm-from { text-align: left; }
    .r-paradigm-arrow { width: 100%; }

    .r-requirement,
    .r-design-item {
      grid-template-columns: 1fr;
      gap: 0.25rem;
    }

    .r-timeline-header,
    .r-timeline-row {
      grid-template-columns: 80px 100px 1fr;
      gap: 0.5rem;
    }

    .r-window-track {
      flex-direction: column;
      gap: 1rem;
      align-items: center;
    }
    .r-window-label-left,
    .r-window-label-right {
      width: 100%;
      text-align: center;
    }
    .r-window-gap {
      position: static;
      transform: none;
    }
  }
</style>
