# Alice Handoff -- 2026-04-24

## What happened today

### 1. Signal pipeline fix

The Phase 1-5 signal integration (38 new dynamical + 3 new motor columns) was blocked by a TypeScript wrapper bug. `libSignalsNative.ts` had two wrapper functions that explicitly constructed return objects with only the original 14 fields, silently discarding everything the Rust engine produced. Three edits to one file fixed it. All new signal columns now populate through the live pipeline.

Backfill completed: 31 dynamical + 31 motor rows re-inserted with all new columns populated at expected coverage rates.

### 2. Extended ghost residuals

Expanded the reconstruction residual comparison from 13 to 41 behavioral dimensions. 28 new signals stored in `extended_residuals_json` (JSONB) on `tb_reconstruction_residuals`, organized by theoretical family. Schema migration 022 applied. `ON CONFLICT` changed to `DO UPDATE` for backfill support. All 180 existing residual rows backfilled from stored seeds.

Key finding: **MF-DFA spectrum width is the most ghost-resistant dimension by 4x.** The ghost cannot reproduce multifractal structure because it generates from a single stochastic process. Ordinal statistics (CECP, forbidden patterns, LZC) are nearly perfectly reproducible. This decomposition was invisible with 13 dimensions.

Per-family residual breakdown (full adversary, journal sessions):

| Family | Mean |Residual| |
|---|---|
| Multifractal | 4.75 |
| Motor Complexity | 1.28 |
| Recurrence Networks | 0.13 |
| Causal Emergence | 0.13 |
| Spectral | 0.11 |
| Dynamic Modes | 0.04 |
| Ordinal | 0.006 |

### 3. Calibration guard

`computeReconstructionResidual` was the only aggregate pipeline function missing a `question_source_id = 3` early-return guard. Added. Full audit confirmed all 5 aggregate systems now guard correctly (profile, cross-session, integrity, semantic baselines, reconstruction). Per-session signals (dynamical, motor, process, semantic) correctly compute for both channels. Verified with a live calibration submission (q89).

### 4. Observatory expansion

**New page: `/observatory/scales`** -- the instrument theory page. Multi-scale portrait (PE spectrum orders 3-7 + MSE curve scales 1-5 with 5-session overlay traces). 9 theoretical family sections with raw time-series trajectories. Ergodicity classification panel (Mangalam et al. 2022).

**3 new API routes:**
- `/api/observatory/scales` -- all 51 dynamical + 3 motor complexity signals per session
- `/api/observatory/cross-session` -- 11 cross-session longitudinal signals (previously had NO API surface)
- `/api/observatory/process` -- 9 process structure signals (previously had NO API surface)

**Updated pages:**
- Entry detail: dynamical panel expanded from 11 to 51 signals organized into 9 named families. New process, cross-session, and discourse coherence panels.
- Trajectory: 17 new time-series charts (cross-session, discourse, process structure).
- Ghost: residual table expanded from 17 to 41+ signals organized by 7 theoretical families.
- All pages: nav bar updated with "scales" link.

Build passes clean.

### 5. Paper updates

**Option C** ("The Quiet Debt") -- Section 8 expanded with 4 new paragraphs: cross-validation architecture, MF-DFA ghost resistance finding, reserve-decline dimensional pattern, ergodicity classification constraint.

**Option F** ("Reconstruction Validity") -- Signal families section expanded to 51 dynamical + 17 motor with citations. Per-family residual table added to Section 6.5. Section 7.1 extended with MF-DFA structural finding.

**Option A** ("A Closing Window") -- v3 note updated: signal counts (51 dyn, 17 motor), cross-validation pairs, INC-012 closure, ghost family breakdown.

**Option G** ("Irreversible Loss") -- New paragraph in Section 5: reanalyzability of raw process data under new signal families without recapture.

### 6. METHODS_PROVENANCE.md

INC-013 added documenting: wrapper bug, extended residuals, calibration guard, per-family residual finding. INC-012 updated with resolution note confirming paradigm-gap closure.

## Current system state

| Component | Status |
|---|---|
| Signal pipeline | Fully operational. 51 dynamical + 17 motor + 9 process + 14 semantic + 11 cross-session signals computed per session. |
| Ghost residuals | 41-dimension behavioral comparison. 5 variants. Extended JSONB per row. |
| Calibration boundary | All 5 aggregate systems guarded. Per-session signals compute for both channels. |
| Observatory | 6 pages (overview, trajectory, scales, coupling, ghost, entry detail). All new signals surfaced. |
| Daily delta | Computing on journal submission. April 23 delta magnitude = 1.107. |
| Papers | A, C, F, G updated with signal expansion findings. |

## What is NOT done (deferred from earlier sessions, unchanged)

- Word frequency IKI residual (needs SUBTLEX-US data file)
- Persistent homology / TDA (~300 lines Rust)
- Circadian rhythm signals (data-gated, needs varied-hour sessions)
- Cognitive microstates / HSMM (Phase 2, ~500 lines)
- Reproducibility snapshot updates for new signals
- Wasserstein distance for cross-session motor trajectory
- Calibration engine parallel pipeline (design spec at `systemDesign/CALIBRATION_ENGINE.md`, deferred)

## Files changed this session

### New
- `src/pages/observatory/scales.astro`
- `src/pages/api/observatory/scales.ts`
- `src/pages/api/observatory/cross-session.ts`
- `src/pages/api/observatory/process.ts`
- `db/sql/migrations/022_extended_residuals.sql`
- `src/scripts/backfill-extended-residuals.ts`

### Modified
- `src/lib/libSignalsNative.ts` -- wrapper fix (the root cause)
- `src/lib/libReconstruction.ts` -- extended residuals, calibration guard
- `src/lib/libDb.ts` -- interface + ON CONFLICT DO UPDATE
- `db/sql/dbAlice_Tables.sql` -- extended_residuals_json column
- `src/pages/api/observatory/entry/[id].ts` -- full dynamical + process/cross-session/discourse
- `src/pages/api/observatory/states.ts` -- process/cross-session/discourse JOINs
- `src/pages/api/observatory/ghost.ts` -- extended_residuals_json in SELECT
- `src/pages/observatory/entry/[id].astro` -- family-grouped panels
- `src/pages/observatory/trajectory.astro` -- 3 new sections, 17 charts
- `src/pages/observatory/ghost.astro` -- extended residual table
- `src/pages/observatory/index.astro` -- nav
- `src/pages/observatory/coupling.astro` -- nav
- `systemDesign/METHODS_PROVENANCE.md` -- INC-013 + INC-012 resolution
- `papers/option_c_draft.md` -- Section 8 expansion
- `papers/option_f_draft.md` -- signal families + results + motor residual
- `papers/option_a_demographic_confound_paper.md` -- v3 note
- `papers/option_g_draft.md` -- reanalyzability paragraph
